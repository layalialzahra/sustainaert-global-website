{
  "name": "sustainacert-performance-dependencies",
  "description": "Recommended dependencies for performance optimization",
  "dependencies": {
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^5.12.0",
    "terser": "^5.27.0",
    "open-cli": "^8.0.0",
    "@lhci/cli": "^0.13.0",
    "web-vitals": "^3.5.1"
  },
  "scripts": {
    "build:analyze": "vite build && npx open-cli dist/stats.html",
    "lighthouse": "lhci autorun --collect.url=http://localhost:4173",
    "perf:test": "npm run build && npm run preview"
  },
  "notes": {
    "clsx": "Utility for constructing className strings conditionally",
    "tailwind-merge": "Merge Tailwind CSS classes without style conflicts",
    "rollup-plugin-visualizer": "Visualize and analyze your bundle",
    "terser": "JavaScript minifier for production builds",
    "open-cli": "Cross-platform command to open generated bundle report",
    "@lhci/cli": "Lighthouse CI for automated performance testing",
    "web-vitals": "Library for measuring real user performance metrics"
  },
  "installation": {
    "required": "npm install clsx tailwind-merge",
    "devTools": "npm install -D rollup-plugin-visualizer terser open-cli",
    "optional": "npm install -D @lhci/cli web-vitals"
  }
}
